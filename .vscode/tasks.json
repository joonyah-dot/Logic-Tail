{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build VST3 (Debug) + Install",
      "type": "shell",
      "command": "powershell",
      "args": [
        "-NoProfile",
        "-ExecutionPolicy",
        "Bypass",
        "-Command",
        "& { cmake --build build --config Debug --target LogicTail_VST3; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; Copy-Item -Recurse -Force \"${workspaceFolder}\\build\\LogicTail_artefacts\\Debug\\VST3\\LogicTail.vst3\" \"C:\\Program Files\\Common Files\\VST3\" }"
      ],
      "problemMatcher": [],
      "group": "build"
    }
  ]
}
